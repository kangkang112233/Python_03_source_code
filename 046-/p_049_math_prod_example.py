import math

# List of book page counts
book_pages = [300, 400, 150, 200]

# Calculate the product of all book page counts
total_pages_product = math.prod(book_pages)

# Print the total product of pages
print(f"Total product of book pages: {total_pages_product}")
